from setuptools import setup, find_packages

setup(
    name = "src",
    version = "0.0.1",
    description = "It is a wine quality project",
    author = "Gourav_Chouhan",
    packages = find_packages(),
    license = "MIT",
    
)
